service secureshell
{
        disable         = no
        flags           = REUSE
        socket_type     = stream
        wait            = no
        user            = secureshell
        server          = /app/secureshell
}
